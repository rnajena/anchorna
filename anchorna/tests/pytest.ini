[pytest]
markers =
    slowtest: mark a test as slow
filterwarnings =
    # ignore DeprecationWarnings and PendingDeprecationWarnings triggered by other modules
    ignore::DeprecationWarning:(?!anchorna).*
    ignore::PendingDeprecationWarning:(?!anchorna).*
